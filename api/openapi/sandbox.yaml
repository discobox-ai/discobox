openapi: 3.1.1
info:
  title: Discobox Sandbox Agent REST API
  version: 0.1.0
  description: |
    Minimal seed contract for the future REST API exposed from inside a managed
    sandbox environment. Worker-local sandbox operation routes are intentionally
    defined by sandbox.json instead.
servers:
  - url: http://sandbox.local
    description: Provider-delegated sandbox endpoint
paths:
  /healthz:
    get:
      operationId: getHealth
      summary: Report sandbox process health.
      responses:
        '200':
          description: Sandbox agent process is alive.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
  /readyz:
    get:
      operationId: getReadiness
      summary: Report sandbox readiness and scheduling availability.
      responses:
        '200':
          description: Sandbox agent is ready to serve sandbox requests.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReadinessResponse'
  /metadata:
    get:
      operationId: getMetadata
      summary: Return sandbox identity metadata.
      responses:
        '200':
          description: Sandbox identity metadata.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetadataResponse'
components:
  schemas:
    HealthResponse:
      type: object
      additionalProperties: false
      required:
        - ok
      properties:
        ok:
          type: boolean
          description: True when the sandbox agent process is alive.
    ReadinessResponse:
      type: object
      additionalProperties: false
      required:
        - ready
        - schedulable
      properties:
        ready:
          type: boolean
          description: True when the sandbox agent is ready.
        schedulable:
          type: boolean
          description: True when the sandbox can accept work.
    MetadataResponse:
      type: object
      additionalProperties: false
      required:
        - projectId
        - sandboxId
        - workerId
      properties:
        projectId:
          type: string
          description: Project that owns the sandbox.
        sandboxId:
          type: string
          description: Sandbox resource ID.
        workerId:
          type: string
          description: Provider-specific worker ID when present.
        publicKey:
          type: string
          description: Public key registered by the sandbox agent when available.
