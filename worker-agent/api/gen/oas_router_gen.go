// Code generated by ogen, DO NOT EDIT.

package workeragentapi

import (
	"net/http"
	"net/url"
	"strings"

	"github.com/ogen-go/ogen/uri"
)

var (
	rn5AllowedHeaders = map[string]string{
		"GET":  "Authorization",
		"POST": "Authorization,Content-Type",
	}
	rn7AllowedHeaders = map[string]string{
		"DELETE": "Authorization",
		"GET":    "Authorization",
		"PATCH":  "Authorization,Content-Type",
	}
	rn8AllowedHeaders = map[string]string{
		"POST": "Authorization,Content-Type",
	}
	rn10AllowedHeaders = map[string]string{
		"POST": "Authorization,Content-Type",
	}
)

func (s *Server) cutPrefix(path string) (string, bool) {
	prefix := s.cfg.Prefix
	if prefix == "" {
		return path, true
	}
	if !strings.HasPrefix(path, prefix) {
		// Prefix doesn't match.
		return "", false
	}
	// Cut prefix from the path.
	return strings.TrimPrefix(path, prefix), true
}

// ServeHTTP serves http request as defined by OpenAPI v3 specification,
// calling handler that matches the path or returning not found error.
func (s *Server) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	elem := r.URL.Path
	elemIsEscaped := false
	if rawPath := r.URL.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
			elemIsEscaped = strings.ContainsRune(elem, '%')
		}
	}

	elem, ok := s.cutPrefix(elem)
	if !ok || len(elem) == 0 {
		s.notFound(w, r)
		return
	}
	args := [3]string{}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/api/project/"

			if l := len("/api/project/"); len(elem) >= l && elem[0:l] == "/api/project/" {
				elem = elem[l:]
			} else {
				break
			}

			// Param: "projectId"
			// Match until "/"
			idx := strings.IndexByte(elem, '/')
			if idx < 0 {
				idx = len(elem)
			}
			args[0] = elem[:idx]
			elem = elem[idx:]

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case '/': // Prefix: "/worker/"

				if l := len("/worker/"); len(elem) >= l && elem[0:l] == "/worker/" {
					elem = elem[l:]
				} else {
					break
				}

				// Param: "workerId"
				// Match until "/"
				idx := strings.IndexByte(elem, '/')
				if idx < 0 {
					idx = len(elem)
				}
				args[1] = elem[:idx]
				elem = elem[idx:]

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case '/': // Prefix: "/sandboxes"

					if l := len("/sandboxes"); len(elem) >= l && elem[0:l] == "/sandboxes" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						switch r.Method {
						case "GET":
							s.handleWorkerListSandboxesRequest([2]string{
								args[0],
								args[1],
							}, elemIsEscaped, w, r)
						case "POST":
							s.handleWorkerCreateSandboxRequest([2]string{
								args[0],
								args[1],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, notAllowedParams{
								allowedMethods: "GET,POST",
								allowedHeaders: rn5AllowedHeaders,
								acceptPost:     "application/json",
								acceptPatch:    "",
							})
						}

						return
					}
					switch elem[0] {
					case '/': // Prefix: "/"

						if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "sandboxId"
						// Match until "/"
						idx := strings.IndexByte(elem, '/')
						if idx < 0 {
							idx = len(elem)
						}
						args[2] = elem[:idx]
						elem = elem[idx:]

						if len(elem) == 0 {
							switch r.Method {
							case "DELETE":
								s.handleWorkerDeleteSandboxRequest([3]string{
									args[0],
									args[1],
									args[2],
								}, elemIsEscaped, w, r)
							case "GET":
								s.handleWorkerGetSandboxRequest([3]string{
									args[0],
									args[1],
									args[2],
								}, elemIsEscaped, w, r)
							case "PATCH":
								s.handleWorkerUpdateSandboxRequest([3]string{
									args[0],
									args[1],
									args[2],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, notAllowedParams{
									allowedMethods: "DELETE,GET,PATCH",
									allowedHeaders: rn7AllowedHeaders,
									acceptPost:     "",
									acceptPatch:    "application/json",
								})
							}

							return
						}
						switch elem[0] {
						case '/': // Prefix: "/st"

							if l := len("/st"); len(elem) >= l && elem[0:l] == "/st" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								break
							}
							switch elem[0] {
							case 'a': // Prefix: "art"

								if l := len("art"); len(elem) >= l && elem[0:l] == "art" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch r.Method {
									case "POST":
										s.handleWorkerStartSandboxRequest([3]string{
											args[0],
											args[1],
											args[2],
										}, elemIsEscaped, w, r)
									default:
										s.notAllowed(w, r, notAllowedParams{
											allowedMethods: "POST",
											allowedHeaders: rn8AllowedHeaders,
											acceptPost:     "application/json",
											acceptPatch:    "",
										})
									}

									return
								}

							case 'o': // Prefix: "op"

								if l := len("op"); len(elem) >= l && elem[0:l] == "op" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch r.Method {
									case "POST":
										s.handleWorkerStopSandboxRequest([3]string{
											args[0],
											args[1],
											args[2],
										}, elemIsEscaped, w, r)
									default:
										s.notAllowed(w, r, notAllowedParams{
											allowedMethods: "POST",
											allowedHeaders: rn10AllowedHeaders,
											acceptPost:     "application/json",
											acceptPatch:    "",
										})
									}

									return
								}

							}

						}

					}

				}

			}

		}
	}
	s.notFound(w, r)
}

// Route is route object.
type Route struct {
	name           string
	summary        string
	operationID    string
	operationGroup string
	pathPattern    string
	count          int
	args           [3]string
}

// Name returns ogen operation name.
//
// It is guaranteed to be unique and not empty.
func (r Route) Name() string {
	return r.name
}

// Summary returns OpenAPI summary.
func (r Route) Summary() string {
	return r.summary
}

// OperationID returns OpenAPI operationId.
func (r Route) OperationID() string {
	return r.operationID
}

// OperationGroup returns the x-ogen-operation-group value.
func (r Route) OperationGroup() string {
	return r.operationGroup
}

// PathPattern returns OpenAPI path.
func (r Route) PathPattern() string {
	return r.pathPattern
}

// Args returns parsed arguments.
func (r Route) Args() []string {
	return r.args[:r.count]
}

// FindRoute finds Route for given method and path.
//
// Note: this method does not unescape path or handle reserved characters in path properly. Use FindPath instead.
func (s *Server) FindRoute(method, path string) (Route, bool) {
	return s.FindPath(method, &url.URL{Path: path})
}

// FindPath finds Route for given method and URL.
func (s *Server) FindPath(method string, u *url.URL) (r Route, _ bool) {
	var (
		elem = u.Path
		args = r.args
	)
	if rawPath := u.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
		}
		defer func() {
			for i, arg := range r.args[:r.count] {
				if unescaped, err := url.PathUnescape(arg); err == nil {
					r.args[i] = unescaped
				}
			}
		}()
	}

	elem, ok := s.cutPrefix(elem)
	if !ok {
		return r, false
	}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/api/project/"

			if l := len("/api/project/"); len(elem) >= l && elem[0:l] == "/api/project/" {
				elem = elem[l:]
			} else {
				break
			}

			// Param: "projectId"
			// Match until "/"
			idx := strings.IndexByte(elem, '/')
			if idx < 0 {
				idx = len(elem)
			}
			args[0] = elem[:idx]
			elem = elem[idx:]

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case '/': // Prefix: "/worker/"

				if l := len("/worker/"); len(elem) >= l && elem[0:l] == "/worker/" {
					elem = elem[l:]
				} else {
					break
				}

				// Param: "workerId"
				// Match until "/"
				idx := strings.IndexByte(elem, '/')
				if idx < 0 {
					idx = len(elem)
				}
				args[1] = elem[:idx]
				elem = elem[idx:]

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case '/': // Prefix: "/sandboxes"

					if l := len("/sandboxes"); len(elem) >= l && elem[0:l] == "/sandboxes" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						switch method {
						case "GET":
							r.name = WorkerListSandboxesOperation
							r.summary = "List worker sandboxes"
							r.operationID = "worker-list-sandboxes"
							r.operationGroup = ""
							r.pathPattern = "/api/project/{projectId}/worker/{workerId}/sandboxes"
							r.args = args
							r.count = 2
							return r, true
						case "POST":
							r.name = WorkerCreateSandboxOperation
							r.summary = "Create worker sandbox"
							r.operationID = "worker-create-sandbox"
							r.operationGroup = ""
							r.pathPattern = "/api/project/{projectId}/worker/{workerId}/sandboxes"
							r.args = args
							r.count = 2
							return r, true
						default:
							return
						}
					}
					switch elem[0] {
					case '/': // Prefix: "/"

						if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "sandboxId"
						// Match until "/"
						idx := strings.IndexByte(elem, '/')
						if idx < 0 {
							idx = len(elem)
						}
						args[2] = elem[:idx]
						elem = elem[idx:]

						if len(elem) == 0 {
							switch method {
							case "DELETE":
								r.name = WorkerDeleteSandboxOperation
								r.summary = "Delete worker sandbox"
								r.operationID = "worker-delete-sandbox"
								r.operationGroup = ""
								r.pathPattern = "/api/project/{projectId}/worker/{workerId}/sandboxes/{sandboxId}"
								r.args = args
								r.count = 3
								return r, true
							case "GET":
								r.name = WorkerGetSandboxOperation
								r.summary = "Get worker sandbox"
								r.operationID = "worker-get-sandbox"
								r.operationGroup = ""
								r.pathPattern = "/api/project/{projectId}/worker/{workerId}/sandboxes/{sandboxId}"
								r.args = args
								r.count = 3
								return r, true
							case "PATCH":
								r.name = WorkerUpdateSandboxOperation
								r.summary = "Update worker sandbox"
								r.operationID = "worker-update-sandbox"
								r.operationGroup = ""
								r.pathPattern = "/api/project/{projectId}/worker/{workerId}/sandboxes/{sandboxId}"
								r.args = args
								r.count = 3
								return r, true
							default:
								return
							}
						}
						switch elem[0] {
						case '/': // Prefix: "/st"

							if l := len("/st"); len(elem) >= l && elem[0:l] == "/st" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								break
							}
							switch elem[0] {
							case 'a': // Prefix: "art"

								if l := len("art"); len(elem) >= l && elem[0:l] == "art" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch method {
									case "POST":
										r.name = WorkerStartSandboxOperation
										r.summary = "Start worker sandbox"
										r.operationID = "worker-start-sandbox"
										r.operationGroup = ""
										r.pathPattern = "/api/project/{projectId}/worker/{workerId}/sandboxes/{sandboxId}/start"
										r.args = args
										r.count = 3
										return r, true
									default:
										return
									}
								}

							case 'o': // Prefix: "op"

								if l := len("op"); len(elem) >= l && elem[0:l] == "op" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch method {
									case "POST":
										r.name = WorkerStopSandboxOperation
										r.summary = "Stop worker sandbox"
										r.operationID = "worker-stop-sandbox"
										r.operationGroup = ""
										r.pathPattern = "/api/project/{projectId}/worker/{workerId}/sandboxes/{sandboxId}/stop"
										r.args = args
										r.count = 3
										return r, true
									default:
										return
									}
								}

							}

						}

					}

				}

			}

		}
	}
	return r, false
}
