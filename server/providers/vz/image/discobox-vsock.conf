# The pool agent listens on AF_VSOCK from inside its container, and the guest
# helpers serve Docker and lifecycle over it. Debian builds the transport as a
# module, so nothing would open a VSOCK socket without this.
vmw_vsock_virtio_transport
