# The agent this image installed, for the pool-cached version store
# (ADR 0114 §7). Read by discobox-agent-store from the base image; nothing
# outside the sandbox reads it, which is why it is a file here rather than a
# field on the image manifest.
AGENT_PACKAGE=@anthropic-ai/claude-code
AGENT_BIN=claude
